import React from "react";

const LoginForm = () => {
  return (
    <div className="modal">
    <span className="close">X</span>
    <p>Login</p>
    <button>Login</button>
</div>
  );
};

export default LoginForm